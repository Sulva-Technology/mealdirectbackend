# Risk Register

| ID    | Risk                                                                                           | Severity | Likelihood | Mitigation                                                                               | Owner      | Verification Status |
| ----- | ---------------------------------------------------------------------------------------------- | -------- | ---------- | ---------------------------------------------------------------------------------------- | ---------- | ------------------- |
| R-001 | API business endpoints do not yet verify Supabase JWTs                                         | Critical | Medium     | JWT verifier, actor context, and guard exist; apply guard to every business route        | Backend    | Partially verified  |
| R-002 | Object-level authorization is not yet enforced in API handlers                                 | Critical | High       | RBAC guard exists; apply authorization decisions to every order/vendor/rider/admin route | Backend    | Open                |
| R-003 | Paystack webhook side effects require live database verification                               | Critical | Medium     | Database-backed event inbox and payment-success function are wired outside test mode     | Backend    | Partially verified  |
| R-004 | Inventory reservation API requires real PostgreSQL concurrency verification                    | High     | Medium     | Order creation route calls the transactional reservation function with idempotency       | Backend    | Partially verified  |
| R-005 | API order/payment/refund/settlement flows are missing critical-path E2E tests                  | High     | High       | Add endpoint flows once handlers exist; keep CI failing for production gates until then  | QA         | Open                |
| R-006 | Operations endpoint uses a temporary operations token instead of Super Admin JWT/RBAC          | High     | Medium     | Replace guard with verified Super Admin authorization                                    | Backend    | Open                |
| R-007 | Supabase CLI and Docker are missing locally, blocking full DB verification                     | Medium   | High       | Install tooling locally and run `pnpm db:ci`                                             | DevOps     | Open                |
| R-008 | Production alerting is documented but not wired to a paging provider                           | Medium   | Medium     | Configure Render/Supabase/Paystack alerts and on-call escalation                         | Operations | Open                |
| R-009 | Settlement automation is endpoint-driven and not yet scheduled                                 | Medium   | Medium     | Super Admin settlement endpoints exist; add cron automation and reconciliation checks    | Backend    | Partially verified  |
| R-010 | Container scanning is configured as a required pipeline stage but scanner choice is not pinned | Medium   | Medium     | Add Trivy or equivalent scanner to CI with high/critical fail threshold                  | DevOps     | Open                |
